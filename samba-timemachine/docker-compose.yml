version: '3.3'

services:
    samba-timemachine:
        image: timjdfletcher/samba-timemachine:latest
        ports:
            - 10445:445
        volumes:
            - backups:/backups:rw
        restart: unless-stopped
        tty: true
volumes:
    backups:
