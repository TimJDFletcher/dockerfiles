version: '3.3'

services:
    samba:
        image: timjdfletcher/samba-timemachine:latest
        ports:
            - 10445:445
        volumes:
            - /WDZBA365/backups/timemachine:/backups:rw
        restart: unless-stopped
        tty: true
